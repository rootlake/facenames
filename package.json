{
  "name": "facenames",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run generate-faces && vite build",
    "preview": "vite preview",
    "generate-faces": "node scripts/generate-faces.js"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "vite-plugin-qrcode": "^0.2.3"
  }
}
